#!/bin/bash
python3 -c "w='water';s='stir';b='bestchol';t=lambda n,base:sum(base.index(c)*len(base)**i for i,c in enumerate(reversed(n)));r=lambda n,base:''.join(reversed([base[n//len(base)**i%len(base)] for i in range(max(1,__import__('math').floor(__import__('math').log(n+1,len(base)))+1))]));print(r(t('$WATER',w)+t('$STIR',s),b))"
